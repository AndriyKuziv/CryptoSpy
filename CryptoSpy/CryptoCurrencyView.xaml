<UserControl x:Class="CryptoSpy.CryptoCurrencyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CryptoSpy"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Label Content="A bit coin" FontSize="30" FontWeight="Bold" HorizontalAlignment="Center"/>

        <StackPanel x:Name="CryptoInfoPanel" Grid.Row="1">
            <StackPanel.Resources>
                <Style TargetType="{x:Type TextBlock}">
                    <Setter Property="FontSize" Value="10"/>
                    <Setter Property="Margin" Value="0 5 0 5"/>
                </Style>
            </StackPanel.Resources>

            <TextBlock>
                <Run Text="{Binding cryptoCurrency.data.priceUsd}"/>
            </TextBlock>
            <TextBlock Text="{Binding volumeUsd24Hr, StringFormat='Volume for the last 24 hours (USD): {0}'}"/>
            <TextBlock Text="{Binding changePercent24Hr, StringFormat='Change in price: {0}%'}"/>
        </StackPanel>
    </Grid>
</UserControl>
